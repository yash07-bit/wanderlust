// Simple JS to add a click effect or track redirection
const exploreBtn = document.getElementById("redirectBtn");

exploreBtn.addEventListener("click", (e) => {
  // You could add a loading spinner here if your listings page takes time to fetch
  console.log("Redirecting to Wanderlust Listings...");
});
